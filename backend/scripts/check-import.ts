// Mock the import like the backend does
import { isStagePredictionLocked } from "../../shared/config";
console.log("imported");
console.log("isStagePredictionLocked('group'):", isStagePredictionLocked("group"));
